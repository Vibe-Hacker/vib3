# Build outputs (keep for faster builds)
# build/

# IDE
.idea/
*.iml

# Android (keep gradle cache for faster builds)
# android/.gradle/
android/build/

# Only exclude specific large files that aren't needed
# Keep commandlinetools zip as it's needed for Android SDK

# Generated files that change frequently
.flutter-plugins-dependencies